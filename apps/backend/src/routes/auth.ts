import express from "express";

const router = express.Router();

//Create / fetch user
// Maintain session
// Return user.id → used as clientId / freelancerId

router.post("/auth/google", );

router.post("/auth/github", );

router.get("/auth/me", );

router.post("/auth/logout", )


module.exports = router;
